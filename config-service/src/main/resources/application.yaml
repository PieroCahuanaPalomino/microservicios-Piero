server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: zipkin-sleuth
          uri: https://github.com/PieroCahuanaPalomino/microservicios-Piero
          search-paths: config-data
  application:
    name: config-service
      